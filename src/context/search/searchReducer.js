import React from 'react';
import {

} from "../types";

const searchReducer = (state, action) => {
  switch(action.type){

    default:
        return {
            ...state
        }
  }
}

export default searchReducer